# Examples audit — 2026-07-07 (workstream C reference)

Generated by the `examples-audit` fan-out workflow (10 agents, 58 files read in full). Durable reference for the conversion tasks in `docs/superpowers/plans/2026-07-07-examples-ux-and-methodology-adoption.md`. Verify a row against the file before trusting it (`decision-memory`: stale notes are worse than none).

- **Totals:** 58 files — {'creates': 44, 'readonly': 9, 'debug': 5}

- **Current cleanup:** {'should_cleanup': 12, 'finally-unconditional-delete': 32, 'none': 11, 'other': 3}

- **Async:** 4; **creates resources:** 46


## Tier 1 — full conversion (finally-unconditional-delete) (32)


### `agent_builder_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| agent_builder ES\|QL tool | kbnpy_example.cluster_indices (module constant TOOL_ID) | client.agent_builder.create_tool | client.agent_builder.delete_tool (force=True) |
| agent_builder agent | kbnpy-example-agent (module constant AGENT_ID) | client.agent_builder.create_agent | client.agent_builder.delete_agent |
| agent_builder conversation | computed at runtime reply.body["conversation_id"] | client.agent_builder.converse | client.agent_builder.delete_conversation |

- **Collision:** Fixed ids TOOL_ID="kbnpy_example.cluster_indices" and AGENT_ID="kbnpy-example-agent" would 409 on re-run if a prior run's finally cleanup was interrupted (e.g. process killed before finally). Under normal completion the finally block deletes both, so ordinarily no collision.
- **Conversion:** Cleanup is an unconditional finally guarded only by tool_created/agent_created flags (no should_cleanup prompt) — the rework should add opt-in keep behavior. Delete ordering matters: agent is deleted BEFORE the tool, and delete_tool uses force=True (tool is referenced by the agent). The converse step is LLM-connector-gated: wrapped in try/except ApiError and prints 'converse skipped (no LLM connector available)' — this is an infra-gated happy path. The conversation is only created and deleted inside that converse try, so it only exists/needs teardown when an LLM connector is present. execute_tool runs the ES\|QL query directly (needs .kibana-event-log* to exist).

### `alerting_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| alerting rule (.es-query) | computed f"kbnpy-alerting-example-{uuid.uuid4().hex[:8]}" (rule_id) | client.alerting.rule.create | client.alerting.rule.delete |
| rule snooze schedule (transient lifecycle state on the rule) | computed at runtime snoozed.body["schedule"]["id"] (schedule_id) | client.alerting.rule.snooze | client.alerting.rule.unsnooze |

- **Collision:** rule_id already includes a uuid4 suffix, so re-runs do NOT collide and there is no 409 risk. tags=["kbnpy-example"] is shared but only used for filtering, not identity.
- **Conversion:** Cleanup is in finally, wrapped in try/except: pass (swallows delete errors). The rule is created with enabled=False, then the lifecycle section enable -> mute_all -> snooze -> unsnooze -> unmute_all -> disable(untrack=True) runs in sequence; the snooze schedule is undone by unsnooze (unsnooze takes rule_id/schedule_id, delete takes id). backfill.find is read-only (per_page=5). Good id hygiene already; rework mainly needs to preserve the ordered lifecycle calls and the schedule_id extracted from the snooze response.

### `apm_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| APM agent configuration | service_name=SERVICE="kbnpy-example-apm-svc", service_environment=ENVIRONMENT="testing" | client.apm.create_or_update_agent_configuration | client.apm.delete_agent_configuration |
| APM deployment annotation | computed at runtime annotation.body["_id"] (service_name="kbnpy-example-apm-svc", version 1.2.3) | client.apm.create_annotation | NONE |
| APM RUM source map | computed at runtime sourcemap_id = uploaded.body["id"] (service kbnpy-example-apm-svc, version 1.2.3, bundle http://localhost/static/js/bundle.js) | client.apm.upload_sourcemap | client.apm.delete_sourcemap |

- **Collision:** SERVICE="kbnpy-example-apm-svc" + ENVIRONMENT="testing" is a fixed identity but the agent config uses create_or_update (upsert), so no 409 on re-run. The annotation has NO delete API (code comment: 'annotations have no delete API; they live in the observability-annotations index'), so every run appends another annotation for the same service/version that is never cleaned up. Source map upload is keyed by service+version+bundle path (effectively an upsert), so re-run generally overwrites rather than 409s.
- **Conversion:** Annotation is a resource with no delete endpoint — it permanently leaks into the observability-annotations index and accumulates across runs; the rework cannot tear it down and should document/minimize it. Cleanup is in finally: source map deleted first (guarded by sourcemap_id is not None), then agent configuration deleted in a try/except: pass. Requires APM/observability infra to be present. Annotation search is date-window gated (start/end 2026-07-01..05 around the hardcoded 2026-07-03 timestamp), so the happy path assertions depend on hardcoded dates. Source map body is a minimal valid v3 map.

### `attack_discovery_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| space (security solution view) | f"kbnpy-attack-discovery-{suffix}" where suffix = uuid.uuid4().hex[:8] | client.spaces.create | client.spaces.delete |
| connector (.gen-ai) | server-generated id (connector.body["id"]); name is f"kbnpy-attack-discovery-conn-{suffix}" | client.connectors.create | client.connectors.delete |
| attack discovery schedule | server-generated id (created.body["id"]); name is f"kbnpy-daily-attack-discovery-{suffix}" | client.attack_discovery.create_schedule | client.attack_discovery.delete_schedule |

- **Collision:** Low collision risk: all names/ids are suffixed with a per-run uuid4().hex[:8], so no fixed id and no generic kbnpy-example-* shared id — re-runs will not 409. The space_id is unique each run.
- **Conversion:** Multi-step ordered setup: space (must be solution="security" so the securitySolutionAttackDiscovery feature is enabled) -> .gen-ai connector (created WITH space_id) -> schedule (created WITH space_id and an alerts_index_pattern derived from space_id). Teardown in finally is UNCONDITIONAL (guarded only by `is not None`, not by should_cleanup) and ordered: delete_schedule, then connectors.delete, then spaces.delete; connector and schedule deletes pass space_id, and the spaces.delete is wrapped in try/except pass to swallow errors. Infra-gated: the connector points at an OpenAI-compatible backend (KBNPY_LMSTUDIO_OPENAI_URL, default http://localhost:1234/v1, coerced to /chat/completions) using secrets apiKey="dummy-key"; the happy path (find/get_generations/discoveries) just prints counts (likely 0) and does not require the LLM to actually be reachable. A rework offering a keep option must add should_cleanup gating while preserving the delete ordering and space-scoping.

### `cases_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| case (with comments as sub-resources) | server-generated id (case.body["id"], collected in created_ids); title fixed "kbnpy-example Suspicious login activity", tags ["kbnpy-example-tag", "security"] | client.cases.create | client.cases.delete |

- **Collision:** Case id is server-generated so no 409 on re-run. But the tag "kbnpy-example-tag" is a generic shared string: the example queries client.cases.find(tags="kbnpy-example-tag") and client.cases.get_tags(owner="cases"), so any leftover cases from prior runs or other examples using the same tag will inflate the reported totals/tag list. The case title is also fixed. Re-running without cleanup accumulates identically-tagged/titled cases.
- **Conversion:** Teardown in finally is UNCONDITIONAL: `if created_ids: client.cases.delete(ids=created_ids)` — a single bulk delete by id list; there is no should_cleanup gate. Comments are sub-resources removed with the case (no separate comment delete). Multi-step with optimistic-concurrency versioning: add_comment then update_comment needs the comment's id+version; update() (status -> in-progress) needs the case version from a prior cases.get(); update() wraps the bulk PATCH /api/cases with an id/version single-case signature and returns a list (updated.body[0]). A rework offering keep must add should_cleanup gating and account for the generic-tag query picking up leftovers.

### `connectors_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| connector (.server-log) | computed: f"kbnpy-connectors-ex-{uuid.uuid4().hex[:8]}" (random per-run suffix) | client.connectors.create | client.connectors.delete |

- **Collision:** No collision risk: the connector id is randomized with a uuid4 hex suffix, so re-runs never 409 and it never collides with other examples. The prefix kbnpy-connectors-ex- is unique to this file.
- **Conversion:** Single sync connector lifecycle. finally block deletes unconditionally but wraps client.connectors.delete in try/except and just prints a 'Cleanup note' on failure, then client.close(). Step 5 executes the .server-log connector (no external infra needed, returns a status). Step 6 fetches the OAuth callback script (9.4.0+). Docstring mentions client.actions is a deprecated alias but the code uses client.connectors throughout. Rework: this is already close to the desired shape; if converting to a should_cleanup() prompt, note the connector id is caller-specified so keeping it and re-running the SAME id would 409 — but current code re-randomizes each run so that's not an issue today.

### `dashboards_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| dashboard (server-assigned id) | computed: created.body["id"] (server-assigned), title "kbnpy-example Team Overview", tag "kbnpy-example-tag" | client.dashboards.create | client.dashboards.delete |
| dashboard (custom/upsert id) | kbnpy-example-custom-id (fixed literal, created via PUT upsert) | client.dashboards.update | client.dashboards.delete |

- **Collision:** The second dashboard uses a FIXED id 'kbnpy-example-custom-id'. Because it is created via update() (PUT upsert), re-running does NOT 409 — PUT just replaces it — but if a run keeps the resource, the id is shared/predictable and could collide with a concurrent example run using the same name. The tag 'kbnpy-example-tag' is a generic shared tag, and the search in step 4 uses query='kbnpy-example*', which can match dashboards created by OTHER kbnpy examples, inflating the reported total.
- **Conversion:** Two resources tracked in created_ids and torn down in a finally loop that deletes each unconditionally (per-item try/except printing failures). Uses emoji in output. Tech-preview Dashboards API. Responses are {id,data,meta} envelopes. Rework must: (a) keep delete order irrelevant (both are independent dashboards), (b) note the fixed 'kbnpy-example-custom-id' should be randomized if the rework wants keep-and-rerun safety, and (c) be aware the kbnpy-example* search/tag filter is broad and will surface other examples' dashboards, so its printed counts are not deterministic.

### `data_views_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| data view | computed: f"kbnpy-dataviews-example-{uuid.uuid4().hex[:8]}" (random per-run suffix); title "kbnpy-example-logs-*" | client.data_views.create | client.data_views.delete |
| runtime field (on the data view) | hello_field (fixed literal, scoped to the parent data view) | client.data_views.create_runtime_field | client.data_views.delete_runtime_field (deleted inline in step 4, before finally; also removed transitively when the parent data view is deleted) |

- **Collision:** No 409 risk on the data view: id carries a random uuid4 hex suffix. The runtime field name 'hello_field' is fixed but it is namespaced under the just-created (unique) data view, so it cannot collide across runs. The data view title 'kbnpy-example-logs-*' is a fixed index-pattern string but titles may repeat, so no uniqueness constraint issue.
- **Conversion:** Multi-step setup: create data view -> update -> update_fields_metadata -> runtime field create/update/get/delete -> read default -> preview_swap_references(from_id=to_id=view_id) which is a dry-run only (creates nothing). Delete ordering: the runtime field is created AND deleted inside the try block (step 4), so the finally only needs to delete the parent data view; deleting the data view would also clean up the runtime field if step 4 hadn't. finally catches NotFoundError (from kibana.exceptions) and swallows it, so a missing data view during cleanup is tolerated. Imports NotFoundError explicitly.

### `detection_engine_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| detection rule (query type, disabled) | kbnpy-example-detection-rule (literal, assigned to rule_id var; server id echoed as created.body['id']) | client.detection_engine.create_rule | client.detection_engine.delete_rule |

- **Collision:** Fixed rule_id 'kbnpy-example-detection-rule'. If a prior run kept the rule (crash before the finally delete ran, or delete failed), re-running 409s on create_rule. The rule name 'kbnpy example rule' is also fixed and matched by find_rules; preview_rule uses a separate throwaway name 'kbnpy example preview'.
- **Conversion:** Multi-step happy path: create_rule -> get_rule -> find_rules -> patch_rule(tags) -> preview_rule -> export_rules -> import_rules(overwrite=True) -> search_alerts, then finally deletes. The finally delete_rule(rule_id) is truly unconditional (rule_id is always set, no None guard, no try/except), so if create_rule raised, the finally still calls delete_rule and can surface a secondary 404. import_rules re-imports the SAME rule_id with overwrite=True, so no second rule accumulates and the single delete covers it. preview_rule returns an ephemeral previewId with no delete endpoint (nothing to tear down). search_alerts is read-only. Rework should gate the create/keep on a cleanup flag and make the teardown tolerant of the create-failed path.

### `endpoint_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| endpoint reusable library script | server-generated UUID from created.body['data']['id'] (stored in script_id); create name is the fixed literal 'kbnpy-example-collect-logs' | client.endpoint.create_script | client.endpoint.delete_script |

- **Collision:** The script id is server-generated (UUID), not a fixed id, so re-runs do NOT 409 by id. The create name 'kbnpy-example-collect-logs' is fixed; if the server does not enforce name uniqueness, orphaned duplicate scripts accumulate across runs whenever cleanup is skipped/crashes rather than causing a 409. No shared kbnpy-example id is reused as a primary key.
- **Conversion:** finally deletes only when script_id is not None (guarded by the creation-success sentinel), so it always deletes what it created but never a phantom. Read-only steps first: get_metadata_list(page_size=20) and get_actions_state(). The response-action happy path is infra-gated: client.endpoint.isolate(endpoint_ids=['example-endpoint-id']) is EXPECTED to raise BadRequestError on a stack with no enrolled Elastic Defend hosts, and the example catches it and prints 'expected without Defend hosts' -- rework must keep asserting/handling that semantic 400 rather than treating it as success (isolate creates no persisted resource to unassign/unisolate here). download_script returns body that may be str or bytes (isinstance check on download.body). No delete endpoint for endpoints/metadata; nothing to unenroll.

### `entity_analytics_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| asset criticality record (host) | host.name = kbnpy-example-host | client.entity_analytics.create_asset_criticality | client.entity_analytics.delete_asset_criticality |
| asset criticality record (user) | user.name = kbnpy-example-user | client.entity_analytics.bulk_upsert_asset_criticality | client.entity_analytics.delete_asset_criticality |
| watchlist (technical preview) | server-generated id from watchlist.body['id'] (stored in watchlist_id); create name is fixed literal 'kbnpy-example-watchlist' | client.entity_analytics.create_watchlist | client.entity_analytics.delete_watchlist |
| monitored privileged user | kbnpy-example-user (same literal as the user asset-criticality id) | client.entity_analytics.create_monitored_user | NONE (no per-user delete; removed by delete_monitoring_engine(data=True)) |
| monitored users added via CSV | kbnpy-example-admin-1, kbnpy-example-admin-2 (literals in the uploaded CSV string) | client.entity_analytics.upload_monitored_users_csv | NONE (no per-user delete; removed by delete_monitoring_engine(data=True)) |
| privilege monitoring engine (singleton) | singleton per space (no id; tracked by monitoring_started bool) | client.entity_analytics.init_monitoring_engine | client.entity_analytics.delete_monitoring_engine (data=True) |

- **Collision:** Generic shared kbnpy-example ids: host.name 'kbnpy-example-host' and user.name 'kbnpy-example-user' are fixed asset-criticality keys, and 'kbnpy-example-user' is ALSO reused as the monitored-user name -- both could collide with any other example that touches the same entity names. find_asset_criticality uses kuery 'id_value: kbnpy-example*' which will sweep in records from other kbnpy-example scripts. Watchlist name 'kbnpy-example-watchlist' is fixed but the watchlist id is server-generated, so re-runs create new watchlists (orphans accumulate if cleanup is skipped) rather than 409ing.
- **Conversion:** create_asset_criticality/bulk_upsert are UPSERTs (refresh='wait_for'), so re-runs do not 409 on the criticality records. Teardown ordering in finally: (1) delete_asset_criticality for host, (2) delete_asset_criticality for user -- BOTH unconditional with no None guard and no try/except, so if setup failed early these can raise 404 and mask the original error; (3) delete_watchlist only if watchlist_id set; (4) delete_monitoring_engine(data=True) only if monitoring_started. The monitored user and the two CSV users have NO individual delete endpoint -- they are torn down only as a side effect of delete_monitoring_engine(data=True), so the rework must keep engine teardown to avoid leaking monitored-user data. Multi-step ordering dependency: init_monitoring_engine must succeed before create_monitored_user / upload_monitored_users_csv / list_monitored_users. get_entity_store_status is read-only (install/uninstall deliberately skipped per the inline comment). Watchlists are 9.4 technical preview.

### `error_handling.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| action connector (.server-log) | server-generated id from response.body['id'] (stored in connector_id); create name is fixed literal 'Conflict Test Connector' | client.actions.create | client.actions.delete |
| action connector (.server-log) -- duplicate second create in example_conflict_error | server-generated id (response.body['id']) that is NEVER captured into connector_id | client.actions.create | NONE (leaked -- id is overwritten in the local `response` var and never deleted) |

- **Collision:** Connector ids are server-generated, so no fixed-id 409. But example_conflict_error deliberately creates TWO connectors named 'Conflict Test Connector' with identical config; only the first id is tracked. Re-running the example accumulates orphan '.server-log' connectors named 'Conflict Test Connector' (the second one every run, plus the first whenever the finally cleanup is skipped). All the other ids in the file ('non-existent-connector-id', 'test-error-handling', 'test-retry', 'non-existent-id') are intentionally non-existent to trigger 404s and create nothing.
- **Conversion:** Primary purpose is demonstrating exception handling, but example_conflict_error does create real connectors. Cleanup is function-scoped, not main-scoped: the connector delete lives in example_conflict_error's own try/finally, guarded by `if connector_id:` and wrapped in try/except pass (best-effort, swallows errors). main()'s finally only calls client.close(). The second connector is a genuine resource leak the rework must capture and delete. Several sub-examples spin up their OWN Kibana clients: example_authentication_error (basic_auth invalid creds against http://localhost:5601), example_connection_error (http://non-existent-host:5601), example_timeout_error (request_timeout=0.001) -- each calls client.close() ONLY on the success path, so on the expected-exception path those clients are not closed (minor client leak). These are infra-gated to localhost:5601 and only assert the semantic error type. example_bad_request_error creates a connector with a missing required field and expects it to fail (no resource). Uses utils telemetry helpers (should_enable_telemetry/configure_example_telemetry/print_telemetry_info/setup_telemetry_cleanup) and a manual exponential-backoff retry loop with time.sleep. example_authorization_error is a pure no-op stub.

### `exception_lists_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| detection exception list | kbnpy-example-trusted-hosts (literal, assigned to list_id var; server id echoed as created.body['id']) | client.exception_lists.create | client.exception_lists.delete (list_id=list_id) |
| exception list item | server-generated item_id from item.body['item_id'] (create name 'Trusted build server') | client.exception_lists.create_item | NONE (no per-item delete; deleted with its parent list) |
| imported exception list copy | server-generated id/list_id (import with as_new_list=True); located at runtime via find() and stored in imported_id | client.exception_lists.import_lists | client.exception_lists.delete (id=imported_id) |

- **Collision:** Fixed list_id 'kbnpy-example-trusted-hosts' -- if a prior run kept the list (crash before finally, or the delete was swallowed), re-running 409s on create. Both the original and the imported copy share the same fixed name 'Trusted hosts (kibana-py example)', which is what find() filters on. The imported copy gets a server-generated list_id (as_new_list=True) so it does not itself 409, but its shared name makes multi-run cleanup ambiguous.
- **Conversion:** Steps: create list -> create_item -> find_items -> get_summary -> export(id+list_id, namespace_type='single') -> import_lists(as_new_list=True) -> find by name to locate the imported copy (picks the first data entry whose id != created.body['id'], stored in imported_id). Teardown in finally: (1) delete original list_id wrapped in try/except pass (failure silently swallowed), (2) delete imported copy by id only if imported_id was found. The item has no independent delete -- it is removed with its parent list, so ordering is fine. Fragility to fix in the rework: the find-based imported-copy identification only picks ONE copy, so if earlier runs left multiple same-named imported copies, only one is cleaned and the rest orphan; also both list and imported copy share a name, so find returns both. Fixed list_id 409 risk on re-run means the rework should either randomize/namespace the list_id or make create tolerate/pre-delete an existing one.

### `fleet_enrollment_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| agent policy | server-generated policy_id = policy.body['item']['id']; created with fixed name 'kbnpy-example-enrollment-policy' | client.perform_request('POST', '/api/fleet/agent_policies', ...) | client.perform_request('POST', '/api/fleet/agent_policies/delete', body={'agentPolicyId': policy_id}) |
| enrollment API key | server-generated key_id = created.body['item']['id']; created with fixed name 'kbnpy-example-key' | client.fleet_enrollment.create_key | client.fleet_enrollment.delete_key |
| Fleet Server service token | service_token.body['name'] (server-generated) | client.fleet_enrollment.create_service_token | NONE |

- **Collision:** Fixed names 'kbnpy-example-enrollment-policy' (agent policy) and 'kbnpy-example-key' (enrollment key). Fleet agent policy names must be unique, so re-running before cleanup (or if cleanup is skipped) will 409 on the policy name. Resource ids themselves are server-generated so ids won't collide. The service token has no delete endpoint and leaks one token per run.
- **Conversion:** Delete ordering: revoke enrollment key before deleting the policy (current order already does key then policy). Service token (create_service_token) has NO delete endpoint -> unavoidable leak each run; rework cannot tear it down. get_uninstall_tokens(...).body['items'][0] will IndexError if the policy has no uninstall token yet. Policy create/delete go through raw perform_request (POST /api/fleet/agent_policies and .../delete) rather than a namespaced method. Multi-step setup (policy -> key -> token reads). finally deletes are guarded only by None checks (no keep/should_cleanup flag). Agent policy name uniqueness -> 409 on re-run if resources kept.

### `fleet_epm_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| installed registry package | PKG = 'tcp' (module constant) | fleet_epm.install_package | fleet_epm.uninstall_package |
| custom integration package | CUSTOM_INTEGRATION = 'kbnpy_example_app' (module constant) | fleet_epm.create_custom_integration | fleet_epm.uninstall_package (force=True) |

- **Collision:** PKG='tcp' is a REAL shared Elastic registry package, not a kbnpy-namespaced id: if the user (or another example) already had 'tcp' installed, this example's install sets installed_pkg=True and cleanup uninstalls it, destroying pre-existing user state. install_package may also behave differently (already-installed) if 'tcp' pre-exists. Custom integration 'kbnpy_example_app' is a fixed name and will 409 (already installed) on re-run if not cleaned up.
- **Conversion:** Infra-gated: requires internet access from Kibana to reach the Elastic Package Registry (https://epr.elastic.co) for get_categories/get_packages/install_package. Uninstall ordering: custom integration first, then the base 'tcp' package (current order). Cleanup is flag-driven (installed_pkg / created_custom booleans) but unconditional once set — no guard against uninstalling a package the user already had. get_package_file returns raw text (manifest.body.splitlines()). Rework should detect pre-existing 'tcp' install and avoid uninstalling it, and/or use a less collision-prone package.

### `fleet_outputs_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| Fleet output (logstash) | server-generated output_id = created.body['item']['id']; name 'kbnpy-example-logstash' (later renamed to 'kbnpy-example-logstash-renamed') | client.fleet_outputs.create_output | client.fleet_outputs.delete_output |
| Fleet proxy | server-generated proxy_id = proxy.body['item']['id']; name 'kbnpy-example-proxy' | client.fleet_outputs.create_proxy | client.fleet_outputs.delete_proxy |
| Fleet Server host | server-generated host_id = host.body['item']['id']; name 'kbnpy-example-fleet-server' (references proxy_id) | client.fleet_outputs.create_fleet_server_host | client.fleet_outputs.delete_fleet_server_host |

- **Collision:** All three use fixed kbnpy-example-* names (kbnpy-example-logstash/-renamed, kbnpy-example-proxy, kbnpy-example-fleet-server). Ids are server-generated so re-runs mint new ids rather than 409, but the names are generic and could visually collide/accumulate if cleanup is skipped. No delete-by-fixed-id 409 risk.
- **Conversion:** Dependency-ordered teardown required: the Fleet Server host references proxy_id, so the host must be deleted before the proxy (current finally order: host -> proxy -> output, correct). All finally deletes are guarded only by None checks (no keep/should_cleanup flag). Works on a plain dev stack (no internet/infra gating); hosts point at example.com placeholders. Multi-step setup with a create-then-update (rename) and get_output_health in the happy path.

### `fleet_policies_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| agent policy | server-generated agent_policy_id = created.body['item']['id']; fixed name 'kbnpy-example-agent-policy' | client.fleet_policies.create_agent_policy | client.fleet_policies.delete_agent_policy (force=True) |
| package policy (udp integration) | server-generated package_policy_id = pkg.body['item']['id']; fixed name 'kbnpy-example-udp-policy' | client.options(request_timeout=120.0).fleet_policies.create_package_policy | client.fleet_policies.delete_package_policy (force=True) |
| installed 'udp' registry package (side effect of create_package_policy) | 'udp' (resolved version udp_version from GET /api/fleet/epm/packages/udp) | implicit: fleet_policies.create_package_policy installs the udp package if needed | NONE |

- **Collision:** Fixed names 'kbnpy-example-agent-policy' and 'kbnpy-example-udp-policy'. Fleet agent policy and package policy names must be unique, so re-running before cleanup (or if cleanup is skipped) will 409 on both names. The ids are server-generated. The 'udp' package installed as a side effect is a real shared registry package and is never uninstalled (leaks / may already exist).
- **Conversion:** Delete ordering: package policy before agent policy (current finally order, both with force=True). The udp package installed as a side effect of create_package_policy is NEVER uninstalled -> leak; and resolving udp_version via GET /api/fleet/epm/packages/udp requires the package to be reachable in the registry (EPR/internet access) -> infra-gated. create_package_policy uses client.options(request_timeout=120.0) because installing udp is slow. finally deletes are guarded only by None checks (no keep/should_cleanup flag). Step 4 upgrade dry-run persists nothing. Fixed policy names -> 409 on re-run if resources kept.

### `lists_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| security value list | kbnpy-example-bad-ips | client.lists.create | client.lists.delete |
| value list items (cascade-deleted with the list) | auto-ids; values 192.0.2.1 (create_item), 198.51.100.1 / 198.51.100.2 (import_items) | client.lists.create_item / client.lists.import_items | NONE (deleted implicitly when client.lists.delete removes the parent list) |
| value list data streams / index (created only if missing) | default .lists / .items data streams (no explicit id) | client.lists.create_index | NONE |

- **Collision:** Uses a fixed literal list id 'kbnpy-example-bad-ips'. If a prior run kept the list (crash before finally, or the NotFoundError-guarded delete failing another way), re-running 409s at client.lists.create. Generic kbnpy-example-* prefix could collide with other examples that create a list with the same id.
- **Conversion:** finally block deletes the list unconditionally but wraps only NotFoundError. Deleting the list cascades to all items, so items need no separate teardown. create_index is called only on NotFoundError and the resulting value-list data streams are never torn down (no delete endpoint used) - acceptable shared infra but the rework should be aware. Import lands asynchronously: a polling loop (up to 20 x 0.5s) waits for total >= 3. export_items returns a parsed list body. get_index_status raising NotFoundError is the documented signal to call create_index.

### `logstash_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| logstash centralized pipeline | kbnpy-logstash-example-pipeline (module constant PIPELINE_ID) | client.logstash.create_or_update | client.logstash.delete |

- **Collision:** Fixed id 'kbnpy-logstash-example-pipeline'. Low collision risk because create_or_update is a PUT upsert - a re-run overwrites rather than 409s. No shared kbnpy-example-* prefix (uses its own kbnpy-logstash-example-* namespace).
- **Conversion:** create_or_update is an idempotent upsert (204 No Content), used both for step 1 (create) and step 3 (update), so re-runs are safe and never 409. Technical Preview API. finally deletes unconditionally but catches broad Exception and only prints on failure (never re-raises). Pipelines stored in Elasticsearch, so no running Logstash needed.

### `maintenance_windows_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| maintenance window | server-generated (created.body['id']); title literal 'kbnpy-example-weekly-maintenance' (renamed to '...-renamed') | client.maintenance_windows.create | client.maintenance_windows.delete |

- **Collision:** The window id is server-generated (read from created.body['id']), so re-runs never 409. The title 'kbnpy-example-weekly-maintenance' is a fixed literal but titles need not be unique, so no collision. Uses the generic kbnpy-example-* prefix in the title only.
- **Conversion:** Requires a Platinum/Enterprise license or trial - infra/license-gated. Start time is computed at runtime as now()+7 days (formatted ...T..:..:..000Z) so the window is 'upcoming' within Kibana's look-ahead horizon; a far-future start would report 'finished'. Steps include archive then unarchive (state transitions, not new resources) and an update that renames + disables (enabled=False). finally deletes only when mw_id is not None but with no try/except around the delete itself, so a delete failure would propagate.

### `observability_ai_assistant_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| action connector (.gen-ai / OpenAI) | server-generated (created.body['id']); name literal 'kbnpy-example-ai-assistant-connector' | client.connectors.create | client.connectors.delete |

- **Collision:** Connector id is server-generated (read from created.body['id']), so re-runs never 409. Connector name 'kbnpy-example-ai-assistant-connector' is a fixed literal but connector names are not required unique - no collision. Generic kbnpy-example-* prefix in the name only.
- **Conversion:** The chat completion (observability_ai_assistant.chat_complete) is a Technical Preview and needs a real LLM connector. Without OPENAI_API_KEY the code deliberately passes secrets={'apiKey': 'dummy-key'} (apiUrl from OPENAI_API_URL or the OpenAI default), so the happy path is infra-gated: the SSE stream just carries an error chunk from the failed LLM call, which the example treats as still demonstrating the request/streaming flow. Response body is raw SSE bytes decoded and split into 'data: {...}' lines terminated by 'data: [DONE]'; persist=False and disable_functions=True. finally deletes the connector only when connector_id is not None, with no try/except around delete. Note the connector is created via the connectors namespace, not the observability_ai_assistant namespace.

### `osquery_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| osquery saved query | requested id 'kbnpy_example_uptime' (underscores); delete uses returned saved.body['data']['saved_object_id'] | client.osquery.create_saved_query | client.osquery.delete_saved_query |
| osquery pack | name 'kbnpy-example-pack'; id is server-generated pack.body['data']['saved_object_id'] | client.osquery.create_pack | client.osquery.delete_pack |
| osquery live query action (transient) | server-generated live.body['data']['action_id'] | client.osquery.create_live_query | NONE (no delete endpoint; fire-and-forget action) |

- **Collision:** The saved query is created with a FIXED requested id 'kbnpy_example_uptime' (note underscores, unlike the hyphenated kbnpy-example-* convention). If a prior run kept it, re-running 409s at create_saved_query. The pack is created by name 'kbnpy-example-pack'; osquery pack names must be unique, so a surviving pack causes a 409 at create_pack on re-run even though the pack's own id is server-generated. Live query action_id is server-generated - no collision.
- **Conversion:** Delete ordering: pack is torn down before the saved query in finally (they are independent, but this order is fine). The saved query is deleted using the saved_object_id returned in the response, NOT the requested 'kbnpy_example_uptime' literal - the rework must keep using the returned id. Pack created with enabled=False (needs policy_ids + enabled=True to actually schedule). Step 4 (create_live_query with agent_all=True) is infra-gated: it needs enrolled Elastic Agents running the Osquery Manager integration, and is wrapped in try/except ApiError so with no agents the happy path only asserts/prints the semantic error. The live query action has no delete endpoint and is left as-is. finally guards each delete with 'if ... is not None' but has no try/except around the delete calls themselves.

### `saved_objects_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| saved_object (tag) | kbnpy-example-savedobj-tag (f"{PREFIX}-tag", PREFIX="kbnpy-example-savedobj") | client.saved_objects.create | client.saved_objects.bulk_delete (force=True) |
| saved_object (dashboard) | kbnpy-example-savedobj-dashboard (f"{PREFIX}-dashboard") | client.saved_objects.create | client.saved_objects.delete (mid-script once without force, then again in finally with force=True) |
| saved_object (tag, bulk x2) | kbnpy-example-savedobj-bulk-0 and kbnpy-example-savedobj-bulk-1 (f"{PREFIX}-bulk-{i}" for i in range(2)) | client.saved_objects.bulk_create | client.saved_objects.bulk_delete (force=True) |

- **Collision:** All ids are FIXED (derived from PREFIX="kbnpy-example-savedobj"): kbnpy-example-savedobj-tag, kbnpy-example-savedobj-dashboard, kbnpy-example-savedobj-bulk-0, kbnpy-example-savedobj-bulk-1. saved_objects.create is not an upsert, so if a prior run kept these (cleanup failed), re-running 409s on create. PREFIX is example-specific so cross-example collision is unlikely, but re-run-after-keep is a real conflict risk.
- **Conversion:** Not a simple create/delete: step 5 deletes the dashboard MID-SCRIPT and restores it via import_objects(overwrite=True) (round trip), and step 6 re-imports without overwrite to force conflicts then resolve_import_errors — this multi-step semantic flow must be preserved. Cleanup splits tags (bulk_delete with force=True) from non-tags (individual delete with force=True) and already wraps each teardown branch in its own try/except so a failed tag delete does not block the dashboard delete. Uses sync Kibana. export() returns an iterable whose last line is the export-details summary.

### `security_ai_assistant_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| security AI assistant conversation | server-generated (created.body["id"]); title="kbnpy example: suspicious login investigation" | client.security_ai_assistant.create_conversation | client.security_ai_assistant.delete_conversation |
| security AI assistant quick prompts (x2) | server-generated ids (bulk.body["attributes"]["results"]["created"]); names "kbnpy example - summarize alerts", "kbnpy example - explain rule" | client.security_ai_assistant.bulk_action_prompts (create=[...]) | client.security_ai_assistant.bulk_action_prompts (delete={"ids": prompt_ids}) |
| connector (.gen-ai / OpenAI, optional, infra-gated) | server-generated (connector.body["id"]); name="kbnpy example - security assistant llm" | client.connectors.create | client.connectors.delete (inner finally) |

- **Collision:** No FIXED ids — conversation, prompt, and connector ids are all server-generated, so re-runs do NOT 409. Fixed values are only human-facing strings (conversation title, two prompt names, connector name); re-running creates DUPLICATES rather than conflicts, so no hard collision but repeated runs pile up duplicate-named prompts/conversations.
- **Conversion:** Infra-gated happy path: the chat_complete (LLM) branch only runs when KBNPY_LMSTUDIO_OPENAI_URL is set; otherwise it prints a skip message. That branch creates its own connector and tears it down in a nested try/finally, and uses client.options(request_timeout=120) for the long call. Cleanup depends on capturing server-generated ids into conversation_id/prompt_ids before the finally runs — if create fails after partial success those stay None/[] and nothing leaks. find_conversations, find_anonymization_fields, get_knowledge_base are read-only. Uses .body accessor throughout. Sync Kibana.

### `security_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| security role | kbnpy-security-example-log-reader (f"{PREFIX}-log-reader", PREFIX="kbnpy-security-example") | client.security.create_or_update_role | client.security.delete_role |
| security roles (bulk x2) | kbnpy-security-example-bulk-a and kbnpy-security-example-bulk-b (f"{PREFIX}-bulk-a", f"{PREFIX}-bulk-b") | client.security.bulk_create_or_update_roles | client.security.delete_role (looped over [role_name, *bulk_names]) |

- **Collision:** Ids are FIXED (PREFIX="kbnpy-security-example"): kbnpy-security-example-log-reader, -bulk-a, -bulk-b. However both create paths are UPSERTS (create_or_update_role, bulk_create_or_update_roles), so re-running after a kept resource does NOT 409 — it silently updates. PREFIX is example-specific; low cross-example collision risk.
- **Conversion:** invalidate_sessions is called but creates NO resource — it is a destructive op deliberately defused with match="query" against a nonexistent username (f"{PREFIX}-nobody"); the comment warns match="all" would log out every user and that it needs superuser. Preserve that safety framing; do not switch to match="all". Cleanup loop catches NotFoundError per role so a missing role is fine. ApiError is caught in the main body's except before finally. Upsert creates mean idempotent re-runs. Sync Kibana.

### `short_urls_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| short URL | id server-generated (created.body["id"]); slug FIXED = "kbnpy-example-dashboards"; locator_id="LEGACY_SHORT_URL_LOCATOR" | client.short_urls.create | client.short_urls.delete |

- **Collision:** The SLUG "kbnpy-example-dashboards" is a FIXED string. Short-URL slugs are unique, so if a prior run kept the resource (cleanup skipped/failed) re-running 409s on create. The id itself is server-generated (no id collision). Risky fixed id: slug "kbnpy-example-dashboards".
- **Conversion:** Short URL API is a technical preview in Kibana 9.4. Cleanup is unconditional in finally, guarded by short_url_id is not None, so a create failure leaves nothing to delete. resolve(slug=...) is read-only. The rework must handle the fixed-slug 409-on-rerun quirk (e.g. unique/suffixed slug or delete-if-exists). Sync Kibana.

### `spaces_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| space | kbnpy-spaces-example (module constant SPACE_ID) | client.spaces.create | client.spaces.delete |
| saved_object (dashboard) | kbnpy-spaces-example-dash (module constant DASHBOARD_ID) | client.saved_objects.create (overwrite=True) | client.saved_objects.delete (force=True) |
| saved_object (index-pattern) | kbnpy-spaces-example-ip (module constant INDEX_PATTERN_ID) | client.saved_objects.create (overwrite=True) | client.saved_objects.delete (force=True) |

- **Collision:** Uses three fixed literal ids: space 'kbnpy-spaces-example', dashboard 'kbnpy-spaces-example-dash', index-pattern 'kbnpy-spaces-example-ip'. These are example-specific (not the generic shared kbnpy-example-* prefix) so cross-example collision is unlikely. Re-run risk: spaces.create has NO overwrite param, so a leftover space would 409 on re-run; the two saved_objects.create calls use overwrite=True so they are re-run safe.
- **Conversion:** Delete ordering: the finally deletes the two saved objects first, then the space; the code comment notes that deleting the space also cascades-deletes objects copied INTO it (the copied dashboard inside SPACE_ID), so those copies need no separate delete. Every teardown is individually wrapped in try/except pass and the whole thing is in finally. Multi-step happy path: create space -> get_all(include_authorized_purposes) -> update (PUT requires id AND name every call) -> create dashboard + copy_saved_objects into space -> create index-pattern + get_shareable_references -> update_objects_spaces to share then unshare (the share is already undone in the happy path). Idempotency quirk: spaces.create lacks overwrite, so rework must delete-before-create or catch conflict.

### `streams_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| wired child stream | logs.ecs.kbnpyexample (computed: CHILD = f'{ROOT}.kbnpyexample', ROOT='logs.ecs') | client.streams.fork | client.streams.delete |
| significant-events query (on the child stream) | kbnpy-example-errors (query_id literal) | client.streams.upsert_query | NONE (no separate delete; removed implicitly when the child stream is deleted) |
| global streams enabled-state toggle (not a discrete resource) | n/a (global streams wiring) | client.streams.enable | client.streams.disable (only if streams were not already enabled before the run) |

- **Collision:** Child stream id is a fixed literal 'logs.ecs.kbnpyexample'; fork has no overwrite so a leftover child from a prior run would 409 on re-run (finally does delete it, but an aborted run could leave it). The significant-events query_id 'kbnpy-example-errors' AND the content_name 'kbnpy-example-pack' both use the generic shared kbnpy-example-* prefix, but they are scoped under the child stream so real collision risk is low; upsert_query is an upsert so the query_id is re-run idempotent.
- **Conversion:** Global-state teardown is the tricky part: the script probes client.streams.get(name=ROOT) up front (catching NotFoundError) to record was_enabled, and only calls streams.disable() in finally if streams were NOT already enabled — the rework must preserve this restore-prior-state logic and not blindly disable. Child stream delete is wrapped in try/except NotFoundError inside finally. Streams is a 9.4 technical preview. API quirks to keep: get_significant_events requires ISO 8601 absolute timestamps (relative 'now-24h' rejected); export_content returns raw ZIP bytes (bytes(exported.body)). The significant-events query has no dedicated delete endpoint — it is cleaned up by deleting the parent stream, so delete ordering is stream-last-covers-all.

### `synthetics_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| synthetics global parameter | server-generated param['id']; key = 'kbnpy-synthetics-example-auth-token' (f'{PREFIX}-auth-token') | client.synthetics.create_param | client.synthetics.delete_param |
| Fleet agent policy | server-generated policy['id']; name = 'kbnpy-synthetics-example-policy' (f'{PREFIX}-policy') | client.perform_request('POST', '/api/fleet/agent_policies') | client.perform_request('POST', '/api/fleet/agent_policies/delete', body={agentPolicyId, force:True}) |
| synthetics private location | server-generated location['id']; label = 'kbnpy-synthetics-example-location' (f'{PREFIX}-location') | client.synthetics.create_private_location | client.synthetics.delete_private_location |
| synthetics HTTP monitor | server-generated monitor['config_id']; name = 'kbnpy-synthetics-example-monitor' (later renamed to '...-monitor-renamed') | client.synthetics.create_monitor | client.synthetics.delete_monitor |

- **Collision:** All four resource ids are SERVER-GENERATED (param['id'], policy['id'], location['id'], monitor['config_id']) so no id-based 409 on re-run. Names/keys all share the PREFIX 'kbnpy-synthetics-example' (example-specific, not the generic kbnpy-example-*). Main re-run risk is the global-parameter key 'kbnpy-synthetics-example-auth-token', which may be uniqueness-constrained and 409 on a second run if a prior param leaked; Fleet agent policy name, private-location label, and monitor name are generally not unique-constrained.
- **Conversion:** Cleanup is in finally, each step guarded by an `if <id>:` None-check (ids initialised to None before the try), and deletes run in dependency order: monitor -> private location -> agent policy -> param. Delete ordering matters (a private location cannot be removed while its agent policy underpins it, and the monitor references the location). Uses raw client.perform_request for the Fleet agent policy on both create (POST /api/fleet/agent_policies) and delete (POST /api/fleet/agent_policies/delete with force=True) — not a typed synthetics method. Multi-step setup: param -> Fleet policy -> private location (backed by that policy) -> monitor -> get_monitors(tags) -> update_monitor rename -> test_monitor. Not infra-gated in the error sense: everything succeeds via API; the on-demand test run (test_monitor returns testRunId) simply never executes because no Elastic Agent is enrolled — no semantic-error assertion, it is a real happy path.

### `timeline_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| Security Timeline (saved object) | server-generated created.body['savedObjectId']; title = 'kbnpy-example-investigation' | client.timeline.create | client.timeline.delete (saved_object_ids=[...]) |
| Timeline investigation note | server-generated note.body['note']['noteId'] | client.timeline.create_note | client.timeline.delete_notes (note_id=...) |
| pinned event | server-generated pinned.body['pinnedEventId']; event_id input = 'example-event-document-id' | client.timeline.pin_event | NONE (no explicit unpin; removed implicitly when the Timeline is deleted) |

- **Collision:** Timeline savedObjectId, noteId, and pinnedEventId are all SERVER-GENERATED, so no id-based 409 on re-run. The Timeline title 'kbnpy-example-investigation' uses the generic kbnpy-example-* prefix but title is not uniqueness-constrained (each create yields a fresh savedObjectId), so titles will silently accumulate duplicates rather than collide. event_id 'example-event-document-id' is a fabricated ES document _id that need not exist for pinning to succeed.
- **Conversion:** Cleanup is split: the note is deleted in the HAPPY PATH (step 4a, client.timeline.delete_notes) BEFORE the finally, while the Timeline delete lives in finally guarded by `if timeline_id is not None`. This means an exception between note creation and the 4a delete (e.g. during export) leaks the note — and the code comment states that deleting the Timeline removes its pinned events but says nothing about notes, so an orphaned note may survive Timeline deletion. Rework should move the note deletion into finally (guarded by note_id) so it always runs. Pinned event has no dedicated unpin/delete call — relies on Timeline deletion. export() returns an iterable of NDJSON lines (consumed via len(list(exported))). Setup order: create Timeline -> create_note (needs timelineId) -> pin_event (needs timeline_id) -> export -> delete_notes; teardown order timeline-last.

### `visualizations_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| Lens metric visualization | server-assigned id (created.body['id']); title is f"{PREFIX}-{uuid.uuid4().hex[:8]}" i.e. "kbnpy-visualizations-example-<8 hex chars>" | client.visualizations.create | client.visualizations.delete |

- **Collision:** Low risk: the visualization id is server-assigned and the title carries a per-run uuid4 suffix, so re-runs do not collide. PREFIX 'kbnpy-visualizations-example' is only used as a search query prefix (query=f"{PREFIX}*"), not as a fixed create id, so no 409 on re-run.
- **Conversion:** Uses create_kibana_client() from utils (unlike the other three files which use get_kibana_config()+Kibana) — sync client. Flow: create -> get -> update (full-body replace, renames to <title>-renamed) -> search by prefix -> delete. Cleanup is in the outer finally, guarded only by 'if viz_id is not None' (i.e. deletes whatever was created, no should_cleanup keep-option) and wraps delete in try/except NotFoundError to swallow already-gone. Rework needs a should_cleanup() gate to optionally keep the viz. Note the search assertion prints results.body['meta']['total'] which may include leftovers from prior kept runs. data_source is an inline data_view_spec for 'logs-*' so create does not require a pre-existing index/data view.

### `workflows_management.py` — creates, cleanup=`finally-unconditional-delete`


| kind | current id | create | delete |
|---|---|---|---|
| workflow | kbnpy-example-workflow (fixed literal WORKFLOW_ID) | client.workflows.create | client.workflows.delete (called with force=True) |
| workflow execution (run/execution log records, byproduct of client.workflows.run) | server-assigned run['workflowExecutionId'] | client.workflows.run | NONE |

- **Collision:** HIGH risk: WORKFLOW_ID is a fixed generic id 'kbnpy-example-workflow' (a shared kbnpy-example-* style name). If a prior run kept the resource (or cleanup was skipped/failed), re-running will 409 on client.workflows.create. The workflow name inside the YAML is also hardcoded to the same value.
- **Conversion:** Multi-step long-running flow: create (id=WORKFLOW_ID, yaml=WORKFLOW_YAML) -> get -> get_all search -> run (returns workflowExecutionId) -> poll get_execution up to 60x1s until terminal status (completed/failed/cancelled/timed_out) -> poll get_execution_logs up to 30x1s until total>=1. Cleanup in outer finally guarded by a 'created' bool flag, deletes with force=True (unconditional once created; no should_cleanup keep-option). Rework must: (a) replace the fixed WORKFLOW_ID with a unique/per-run id or make create idempotent to avoid 409 on re-run; (b) note the workflow execution + its log records are a byproduct with NO delete endpoint (deleting the workflow with force=True is the only teardown); (c) handle the possibility that execution never reaches terminal status within 60s or logs never index within 30s (loop just falls through, and 'logs' would be referenced after the loop). Sync Kibana client.

## Tier 1 — full conversion (other cleanup shape) (2)


### `fleet_agents_management.py` — creates, cleanup=`other`


| kind | current id | create | delete |
|---|---|---|---|
| bulk agent action (request diagnostics) | server-generated action_id = created.body['actionId']; targets a hardcoded nonexistent agent id 'kbnpy-fleet-agents-example-missing-agent' | client.fleet_agents.bulk_request_diagnostics | client.fleet_agents.cancel_action |

- **Collision:** No persistent named resource is created. The bulk action_id is server-generated (no collision). The targeted agent id 'kbnpy-fleet-agents-example-missing-agent' is intentionally nonexistent and is never created, so no 409/collision risk. Re-runnable as-is.
- **Conversion:** Borderline creates/readonly: sections 1-3 are pure reads (get_setup_status, get_all, get_status, get_tags, get_available_versions). Teardown (cancel_action) is done INLINE in the try block, NOT in finally, so if an exception occurs between bulk_request_diagnostics and cancel_action the action is left un-cancelled; finally only calls client.close(). Infra-gated happy path: get_available_versions().body['items'][0] will IndexError if the versions list is empty; get_all/get_status assume empty listings on a dev stack. initiate_setup() is called unconditionally (idempotent setup, no teardownable resource). Rework should move cancel into a guarded teardown and guard the [0] index accesses.

### `slos_management.py` — creates, cleanup=`other`


| kind | current id | create | delete |
|---|---|---|---|
| SLO (Service Level Objective) | name = f"kbnpy-slos-example-{uuid.uuid4().hex[:8]}" (module-level SLO_NAME); server-assigned id read from created.body['id'] into slo_id | client.slos.create | client.slos.bulk_delete (primary, async task + bulk_delete_status polling) with client.slos.delete as finally fallback |

- **Collision:** Low collision risk: SLO name carries a per-run uuid4 hex[:8] suffix, and the SLO id is server-assigned, so re-runs will not 409. The KQL indicator references index 'kbnpy-example-logs-*' but does NOT create it (read-only reference; the SLO can be created even if the index is absent). No fixed shared id.
- **Conversion:** Teardown is NOT should_cleanup — it always tears down (no human prompt). Primary path (step 5) does an ASYNCHRONOUS bulk_delete then polls client.slos.bulk_delete_status(task_id) in a 60s deadline loop (2s sleeps), setting slo_id=None on completion. The finally block is a CONDITIONAL fallback: `if slo_id is not None` it calls client.slos.delete (single delete) — so it only fires if the bulk-delete task did not finish in time. Requires a Platinum-level or trial license (create/enable/disable/SLO APIs will error otherwise). Multi-step happy path: create -> get -> find(kql) -> find_definitions -> update(partial) -> disable -> enable -> bulk_delete -> poll. Does NOT use create_kibana_client()/telemetry helpers — instantiates kibana.Kibana directly from get_kibana_config(), catches kibana.exceptions.ApiError. If a rework moves to interactive keep/cleanup, note the bulk-delete-vs-fallback duality and the license gate.

## Tier 2 — already prompt; namespacing + idempotency pass (12)


### `actions_management.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| connector (.index) | server-generated UUID; name is fixed literal "Example Index Connector" (id captured as connector["id"], tracked in created_connector_ids) | client.actions.create | client.actions.delete |
| connector (.webhook) | server-generated UUID; name is fixed literal "Example Webhook Connector" | client.actions.create | client.actions.delete |
| connector (.server-log) | server-generated UUID; name is fixed literal "Example Server Log Connector" | client.actions.create | client.actions.delete |
| elasticsearch index (side effect of executing index connector) | kibana-connector-example | client.actions.execute (writes docs via index connector params.documents) | NONE |

- **Collision:** Connectors get server-generated UUID ids, so no 409 on re-run. But the three connector NAMES are fixed literals ("Example Index Connector", "Example Webhook Connector", "Example Server Log Connector") and Kibana allows duplicate names, so re-running while keeping resources silently accumulates duplicate connectors rather than erroring. The index connector writes into a fixed ES index "kibana-connector-example" that is never removed.
- **Conversion:** Cleanup (cleanup_connectors -> should_cleanup prompt) runs INSIDE the try block, NOT in finally; the finally only calls client.close(). If any step raises, cleanup is skipped and created connectors leak. delete_connector() has its own verify-after-delete logic that treats a subsequent get() failure as success. update_connector requires re-supplying config (fetches current first). Executing the index connector writes real docs to ES index kibana-connector-example (no teardown). Heavy telemetry/logging setup wraps main. Ids are discovered at runtime (not caller-chosen), so the rework must keep collecting connector["id"] into a tracked list.

### `async_example.py` (async) — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| connector (.server-log) | computed f"async-example-{uuid.uuid4().hex[:8]}" as name (connector_name); id captured as connector["id"] | client.actions.create | client.actions.delete |
| space | computed f"async-example-{uuid.uuid4().hex[:8]}" (space_id) | client.spaces.create | client.spaces.delete |
| saved object (type=config) | computed f"async-example-{uuid.uuid4().hex[:8]}" (object_id) | client.saved_objects.create | client.saved_objects.delete |

- **Collision:** All three ids/names use a uuid4().hex[:8] suffix, so re-runs do not collide and there is no 409 risk. The saved object uses type="config" with buildNum (99999/88888) — creating an extra 'config' saved object is unusual but the random id avoids clobbering the real Kibana config.
- **Conversion:** Each demonstrate_* function does its own should_cleanup prompt and returns the surviving id, BUT main() discards those return values (await demonstrate_actions/spaces/saved_objects with no assignment), so any 'kept' resource is never re-collected or torn down later — it just leaks. Every demonstrate_* is wrapped in a broad try/except that prints traceback and swallows the error, so a mid-demo failure can leave a created resource undeleted with no top-level cleanup. Uses async context manager only for client.close(), not resource teardown. demonstrate_actions delete path has verify-after-delete via NotFoundError. Concurrent-operations and options-pattern demos are read-only. Rework should hoist a central tracked-resource list + guaranteed teardown instead of per-function prompts whose results are dropped.

### `async_index_connector.py` (async) — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| index connector (.index action) | server-generated id (connector["id"]); connector name is the fixed expression f"Async Index Connector - {index_name}" with index_name default "miconnectedindex" | client.actions.create | client.actions.delete |

- **Collision:** No kbnpy-example-* generic id. The connector id is server-generated so re-runs never 409. However the connector name ("Async Index Connector - miconnectedindex") and the target ES index "miconnectedindex" are fixed/shared (the same index is also used by connector_management.py); Kibana allows duplicate connector names, so re-running without cleanup silently accumulates duplicate connectors all writing to the same index.
- **Conversion:** Cleanup (should_cleanup + actions.delete) sits INSIDE the try block on the happy path, NOT in finally; the finally only awaits client.close(). Any exception mid-run leaks the connector. The delete has bespoke handling: DELETE can return an empty body causing a JSON parse error, so it re-does actions.get() and treats a failed get as confirmation of deletion — the rework must preserve this empty-response quirk. Async: uses create_async_kibana_client() and await throughout; teardown must be awaited. Also uses deprecated datetime.utcnow(). Documents are written via actions.execute (not a persisted resource needing teardown).

### `connector_management.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| index connector (.index action) | server-generated id (connector["id"], stored on self.connector_id); connector name is the fixed f"Index Connector - {self.index_name}" with index_name="miconnectedindex" | self.client.actions.create (via create_or_find_connector) | self.client.actions.delete (via delete_connector) |

- **Collision:** No kbnpy-example-* id. Connector id is server-generated (no 409). The connector name "Index Connector - miconnectedindex" and index "miconnectedindex" are fixed and the index is shared with async_index_connector.py. create_or_find_connector catches ConflictError and, failing that, _find_existing_connector() matches the FIRST connector by exact name — so leftover duplicates from prior runs can be silently reused. Re-running without cleanup can accumulate duplicate connectors of the same name.
- **Conversion:** Class-based (IndexConnectorManager). Idempotency is built in: create_or_find_connector reuses on ConflictError and by name-match via get_all(); delete_connector swallows NotFoundError. Cleanup (should_cleanup + delete_connector) is INSIDE the try happy path, NOT in finally; the finally only calls manager.close() (client close), so an exception mid-run leaks the connector. Delete has the same empty-DELETE-response guard as async_index_connector.py: on error it re-does actions.get() and treats a failed get as confirmation. update_connector requires re-passing name. Uses deprecated datetime.utcnow(). Rework should move cleanup to a finally/should_cleanup pattern and reconcile the shared connector-name/index with async_index_connector.py.

### `simple_alerting_rules.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| alerting rule (.index-threshold) | server-generated (create_response.body["id"]); name "High Request Count" -> updated to "High Request Count (updated)" | client.alerting.rule.create | client.alerting.rule.delete (gated behind should_cleanup prompt) |

- **Collision:** No FIXED id — rule id is server-generated, so re-runs never 409. Rule name "High Request Count" is fixed but is NOT prefixed with kbnpy-example and alerting rules allow duplicate names, so repeated runs pile up duplicate-named rules rather than conflicting. Naming is generic and un-prefixed — rework may want to standardize.
- **Conversion:** Cleanup is interactive: should_cleanup("Delete the rule? (y/N): "); if the user declines the rule is KEPT (fine, server id). Delete has a verify-after-failure fallback — on delete exception it re-GETs the rule and treats a failed GET as confirmed deletion (works around empty-DELETE-body JSON parse errors). Heavy telemetry scaffolding (configure_example_telemetry / setup_telemetry_cleanup / logging.extra) surrounds the logic. update happens before cleanup. Sync client via create_kibana_client(). No infra gating.

### `simple_index_connector.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| connector / action (.index) | server-generated (connector_response.body["id"]); name "My Connected Index Connector"; config.index "miconnectedindex" | client.actions.create | client.actions.delete (gated behind should_cleanup prompt) |
| Elasticsearch index (side effect of actions.execute) | miconnectedindex (FIXED, config.index) | client.actions.execute (writes a document, auto-creates the index) | NONE |

- **Collision:** No FIXED connector id (server-generated); connector name "My Connected Index Connector" is fixed and un-prefixed (no kbnpy-example prefix) but connectors allow duplicate names, so no 409 — repeated runs create duplicate connectors. The FIXED ES index "miconnectedindex" persists across runs and is never cleaned up; re-runs just append documents to it.
- **Conversion:** Two resources with asymmetric teardown: the .index connector is deleted (behind should_cleanup), but the ES index "miconnectedindex" it writes into has NO delete path in this script (kibana-py actions API does not remove the index) — it leaks by design. Delete has the same verify-after-failure fallback (re-GET, failed GET = confirmed deletion, handling empty-DELETE-body). Uses client.actions.execute to write a document (side effect), then actions.get to verify. Telemetry scaffolding present. Sync client via create_kibana_client(). Names are generic/un-prefixed — rework may want kbnpy-example naming for both connector and index.

### `simple_saved_object.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| saved-object (index-pattern / data view) | kibana-py-apm-traces | client.saved_objects.create(type="index-pattern") | client.saved_objects.delete(type="index-pattern") |
| saved-object (visualization) | my-test-viz | client.saved_objects.create(type="visualization") | client.saved_objects.delete(type="visualization") |

- **Collision:** Uses two FIXED ids, not the kbnpy-example-* pattern: 'kibana-py-apm-traces' (index-pattern) and 'my-test-viz' (visualization). Both are hardcoded, so re-running the example after choosing to KEEP the objects will 409 on create. No uuid/timestamp uniqueness.
- **Conversion:** Delete ordering matters: the visualization references the index-pattern via `references=[{type:index-pattern,id:kibana-py-apm-traces}]`, and the code deletes the visualization FIRST, then the data view. Cleanup is gated behind should_cleanup(); there is NO finally teardown (finally only logs + client.close()), so declining the prompt or an exception leaves both objects behind. Each delete is wrapped in a verify-on-error pattern (delete, then get() to confirm — some DELETEs return empty bodies that raise). Sync client via create_kibana_client(); uses telemetry helpers.

### `simple_space.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| space | my-team-space | client.spaces.create | client.spaces.delete |

- **Collision:** Uses FIXED id 'my-team-space' (not kbnpy-example-*). Re-running after keeping the space will 409 on client.spaces.create. No uniqueness suffix.
- **Conversion:** Single space created. Cleanup gated behind should_cleanup(); NO finally teardown (finally only logs + client.close()), so declining or an early exception leaks the space. Delete wrapped in verify-on-error pattern (delete then spaces.get() to confirm, because empty DELETE bodies can raise). Sync client via create_kibana_client(); uses telemetry helpers.

### `space_management.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| space | marketing-team | self.client.spaces.create (via SpaceManager.create_space) | self.client.spaces.delete (via SpaceManager.delete_space / cleanup) |
| space | engineering-team | self.client.spaces.create (via SpaceManager.create_space) | self.client.spaces.delete (via SpaceManager.delete_space / cleanup) |

- **Collision:** Two FIXED ids: 'marketing-team' and 'engineering-team' (not kbnpy-example-*). On a full re-run after keeping spaces, step 2's create of 'marketing-team' raises ConflictError and aborts the whole flow into the except block. Note: the example INTENTIONALLY re-creates 'marketing-team' in step 7 to demo ConflictError handling, and intentionally gets 'nonexistent-space' to demo NotFoundError — those are expected, not bugs.
- **Conversion:** OO wrapper: SpaceManager tracks self.created_spaces (append only on successful create; the intentional duplicate create in step 7 raises ConflictError before appending, so it is not double-tracked). cleanup() iterates created_spaces and calls delete_space, which itself verifies deletion via a follow-up get() and retries on error. Cleanup gated behind should_cleanup(); NO finally teardown (finally only client.close()), so declining or an early failure leaks both spaces. Uses create_kibana_client() + telemetry helpers; imports ConflictError/NotFoundError from kibana.exceptions. Rework should preserve the deliberate error-handling demo (steps 7) while making the two fixed ids re-run-safe.

### `space_scoped_connector.py` — creates, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| space | f"team-space-{int(time.time())}" (timestamp-suffixed, computed at runtime) | client.spaces.create | client.spaces.delete (in interactive_cleanup; cascades to all space-scoped connectors) |
| connector / action (.index type), primary | name f"Team Connector ({space_id})"; server-assigned id read from connector['id'] | client.actions.create(space_id=space_id) | client.actions.delete(id=connector_id, space_id=space_id) |
| connector / action (.index type), secondary 'space-scoped-client' demo | name f"Space-Scoped Connector ({space_id})"; server-assigned id from connector['id'] | space_client.actions.create (via client.space(space_id)) | NONE |

- **Collision:** Space id is timestamp-based (f"team-space-{int(time.time())}"), so generally unique across runs — but two runs within the same wall-clock second could collide (409). Not the kbnpy-example-* pattern. Connector ids are server-assigned (no collision). Two ES indexes are written via connector execution ('{space_id}-data' and '{space_id}-scoped-data') and are NOT Kibana-managed here.
- **Conversion:** KEY LEAK: the second/secondary connector created in demonstrate_space_scoped_client() is never individually deleted — interactive_cleanup() only knows the primary connector_id. If the user declines space deletion but chooses 'delete just the primary connector', the space-scoped connector is orphaned (the code even prints a note admitting this). Only deleting the SPACE removes both connectors (cascade). Additionally, the ES indexes written by client.actions.execute() are never cleaned up (no ES delete endpoint touched here). Cleanup is nested should_cleanup() prompts (space-or-nothing, then connector-or-nothing); NO finally teardown (finally only client.close()). Multi-step: create space -> create connector -> execute -> display -> space-scoped-client demo (create+execute a 2nd connector) -> interactive cleanup. Uses create_kibana_client() + telemetry helpers and the client.space(space_id) scoped-client pattern (module has no `import logging` at top — it is imported locally inside functions, and main references ConnectionError). Rework must track BOTH connector ids for reliable teardown.

### `debug_connector.py` — debug, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| connector (.index) | server-assigned, extracted as connector_data["id"]; name is the fixed literal "Debug Index Connector" | client.actions.create | client.actions.delete (only if should_cleanup() returns True; NOT deleted in finally) |

- **Collision:** No 409 collision: the connector id is server-assigned (not caller-specified), so repeated runs get new ids. The name 'Debug Index Connector' is fixed, so declining cleanup and re-running produces multiple connectors with the same NAME but distinct ids (name is not unique) — clutter, not a hard failure.
- **Conversion:** Uses the DEPRECATED client.actions alias (should map to client.connectors in the rework). Delete is gated behind should_cleanup() inside the try block; the finally ONLY calls client.close(), so if the user declines OR any exception occurs after create but before the prompt, the connector LEAKS (no unconditional teardown). The delete path has bespoke 'confirm-by-GET' error handling: on delete exception it does a get() and treats a subsequent get() failure as successful deletion (works around empty-body DELETE JSON parse errors). Config targets index 'miconnectedindex'; index-connector creation does not require the index to exist, so this is not infra-gated. Heavy telemetry/observability boilerplate (configure_example_telemetry, setup_telemetry_cleanup). Requires the .index connector type to be present or it early-returns.

### `debug_saved_objects.py` — debug, cleanup=`should_cleanup`


| kind | current id | create | delete |
|---|---|---|---|
| saved object (visualization) | computed: f"debug-test-{uuid.uuid4().hex[:8]}" (random per-run suffix) | client.saved_objects.create | client.saved_objects.delete (only if should_cleanup() returns True; NOT deleted in finally) |

- **Collision:** No 409 collision: id uses a random uuid4 hex suffix and does not follow the kbnpy-example-* scheme (prefix is 'debug-test-'), so it won't collide with other examples either.
- **Conversion:** IMPORTANT quirk: the create() call is nested inside a create_span(...) context wrapped by 'try: ... except Exception: pass' (lines ~87-137), so a failed CREATE is SILENTLY swallowed — the very next step get() would then fail and fall through to the outer except. Delete is gated behind should_cleanup() and the finally only does client.close(), so declining cleanup (or any error before the prompt) LEAKS the object (no unconditional teardown). Includes a deliberate version-conflict demonstration (step 4) that ASSERTS the update fails (semantic-error happy path — success there is treated as unexpected). Delete path uses the same 'confirm-by-GET' pattern as debug_connector to tolerate empty-body DELETE responses. Heavy telemetry/structured-logging boilerplate. Rework should move creation out of the exception-swallowing span block and add deterministic teardown.

## Tier 3 — read-only / debug; consistency check only (12)


### `debug_spaces.py` — debug, cleanup=`none`

- **Collision:** None — read-only inspection script. It only calls client.spaces.get_all() and prints; it never creates, updates, or deletes any server-side resource, so there are no ids that could collide or 409.
- **Conversion:** Pure read-only/inspection: lists all spaces and dumps each space's fields plus full JSON. No teardown needed; finally only logs and calls client.close(). Two caveats for the rework: (1) the ImportError fallback branch (OpenTelemetry unavailable, lines ~233-256) is INCOMPLETE — it re-fetches spaces but stops with a comment '# ... rest of the logic would be similar', so without OTel the detailed per-space output never prints; (2) access URLs are HARD-CODED to http://localhost:5601 regardless of the actual configured kibana_url. Heavy telemetry/structured-logging boilerplate. Since nothing is created, no should_cleanup()/finally-delete work is required — the rework is purely UX/output cleanup.

### `debug_status.py` — debug, cleanup=`none`

- **Collision:** No server-side resources are created, so there are no ids that can collide or 409. The only fixed identifiers are telemetry span names (kibana_debug_status_check) and the logger name, which are irrelevant to Kibana state.
- **Conversion:** Pure inspection/troubleshooting script: it only calls client.status.get_status() and client.status.get_stats() and pretty-prints server info, version, core-service levels, plugin levels, process/OS/memory stats. No creates, no deletes. finally only does client.close(). Heavy on optional infrastructure: uses utils telemetry helpers (should_enable_telemetry, configure_example_telemetry with logs_enabled, print_telemetry_info, setup_telemetry_cleanup) and demonstrate_structured_logging/demonstrate_log_trace_correlation, plus an OpenTelemetry create_span wrapped in try/except ImportError that falls back to running display_status/display_stats without a span. Rework must preserve the ImportError fallback and the telemetry-gated log-forwarding demo block; there is nothing to tear down beyond closing the client.

### `task_manager_management.py` — debug, cleanup=`none`

- **Collision:** None — creates nothing.
- **Conversion:** Read-only diagnostic: single client.task_manager.health() call, then prints overall status, per-section monitored stats, configuration, workload, and runtime drift percentiles. Classified debug rather than readonly because it is a Task Manager health/troubleshooting report rather than a resource listing. No teardown. Fragility for the rework: assumes presence of nested keys health['stats']['configuration']['value'], ['workload']['value'], ['runtime']['value'] and would KeyError if a section is absent (drift is defensively .get()-guarded, the others are not). Chooses api_key over basic_auth when both present.

### `async_comprehensive.py` (async) — readonly, cleanup=`none`

- **Collision:** No server-side resources created; no ids at risk.
- **Conversion:** Purely read-only: fetch_status (perform_request GET /api/status), spaces.get_all, saved_objects.find(type=dashboard). Uses async context manager (async with create_async_kibana_client()) for automatic close — no explicit teardown needed. Runs the three fetches concurrently via asyncio.gather, then demonstrates the options() pattern with a custom 5s timeout (also read-only GET /api/status). Does sys.path.insert to import from examples.utils. Heavy telemetry setup. Nothing to clean up.

### `async_simple_status.py` (async) — readonly, cleanup=`none`

- **Collision:** None. Creates no server-side resources.
- **Conversion:** Pure read: a single client.perform_request("GET", "/api/status") plus printing overall level, version, build, and first 5 service statuses. Uses `async with create_async_kibana_client() as client:` so the client is closed automatically by the async context manager — no resource teardown needed. Note it imports utils via `from examples.utils import ...` (adds parent dir to sys.path) rather than the bare `from utils import ...` other files use; the rework should keep imports consistent.

### `basic_usage.py` — readonly, cleanup=`none`

- **Collision:** None. No server-side resources are created.
- **Conversion:** Six demo functions showing client construction/config only. Server calls are limited to read-only client.actions.list_types() / (via other examples) list_types — no create/update. example_basic_initialization builds clients with placeholder secrets (api_key="your_api_key", bearer_auth="your_bearer_token") and immediately close()s them without making requests. example_multiple_hosts points at fake hosts (kibana-node-1/2/3) and never connects. Every client is closed via close() or a context manager. No teardown of resources needed; the rework is UX/wording only.

### `fleet_management.py` — readonly, cleanup=`none`

- **Collision:** No resources created. Uses a fixed id 'kbnpy-fleet-example-host' only in health_check, which is expected to 404 (no resource is created), so no collision risk.
- **Conversion:** Not purely read: step 4 mutates a global Fleet setting (update_settings prerelease_integrations_enabled = not prerelease) then restores it with a second update_settings. There is NO finally/try around the restore, so an exception between toggle and restore leaves the global setting flipped (state leak) — rework should restore in a finally or via a guarded teardown. Step 6 is an infra-gated happy path that deliberately asserts a semantic error: health_check(id='kbnpy-fleet-example-host') is caught as NotFoundError as the expected outcome on a dev stack. setup() is idempotent. No delete/teardown of persistent resources needed.

### `ml_management.py` — readonly, cleanup=`none`

- **Collision:** No resources created. Deliberately references nonexistent ids 'kbnpy-example-nonexistent-job' and 'kbnpy-example-nonexistent-model' so the spaces-update calls are no-ops; these cannot collide because they are expected not to exist.
- **Conversion:** Every call is intentionally a no-op / read-shape demo: ml.sync(simulate=True) reports what WOULD change without doing it (the real client.ml.sync() is only shown as a comment); update_jobs_spaces and update_trained_models_spaces are passed nonexistent ids so they mutate nothing and just exercise the per-item success/error response shape. This is an infra-gated happy path - real success entries require pre-existing ML anomaly-detection jobs / trained models. finally only calls client.close(); nothing to tear down. Category is readonly (no creation, no teardown) though technically it invokes write endpoints in a guaranteed no-op mode.

### `simple_status.py` — readonly, cleanup=`none`

- **Collision:** None — no resources created, no ids used.
- **Conversion:** Pure read-only health check: single call client.status.get_status(), then prints name/version/overall level and branches on available/degraded/unavailable. No teardown needed (finally only logs + client.close()). Reads status['name'], status['version']['number'], status['status']['overall']['level' / 'summary'] — rework must keep those key paths. Sync client via create_kibana_client(); uses telemetry helpers.

### `status_management.py` — readonly, cleanup=`none`

- **Collision:** None — creates nothing.
- **Conversion:** Pure read-only: get_status (v8 and v7format=True), get_stats, get_features. No teardown. Only fragility for the rework is deep nested-key access that assumes a stable response shape (status['status']['core'], status['status']['plugins'], stats['process']['memory']['heap'], stats['response_times']['avg_ms']); get_features is a 9.4 technical preview. Chooses api_key over basic_auth when both present.

### `upgrade_assistant_management.py` — readonly, cleanup=`none`

- **Collision:** No ids used; pure read of cluster upgrade-readiness status. No collision risk.
- **Conversion:** Single read-only call client.upgrade_assistant.status(); prints readyForUpgrade, details, ES deprecation log count, and Kibana API deprecations. Sync Kibana client (api_key or basic_auth branch), closed in a finally. API is Technical Preview in 9.4 so the rework may want to tolerate a not-enabled/permission error. Nothing to create or tear down.

### `uptime_management.py` — readonly, cleanup=`other`

- **Collision:** No resource ids; mutates global singleton uptime settings only. No 409/collision risk, but note it writes to shared cluster-wide uptime settings so concurrent runs could interfere.
- **Conversion:** Not a create/delete script: it reads uptime settings, applies a partial update (cert_expiration_threshold=14, cert_age_threshold=365), then RESTORES the captured originals (heartbeatIndices, certExpirationThreshold, certAgeThreshold, defaultConnectors, defaultEmail) in an inner finally. This is why current_cleanup is 'other' — the finally restores prior settings rather than deleting a resource, and the restore runs unconditionally even if the update failed (restoring values that were never changed, which is harmless). Rework must preserve the read-mutate-restore idempotent pattern; there is no should_cleanup gate and no resource to keep.
